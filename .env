# =================================================================
# === APPLICATION CONFIGURATION (.env.example) ===
# =================================================================

# Flask secrets
SECRET_KEY=change-me-dev-secret
JWT_SECRET_KEY=change-me-jwt-secret

# Server
FLASK_ENV=development
PORT=8080
HOSTNAME=medical-app-local

# =================================================================
# === CLOUD SQL CONFIGURATION (REQUIRED) ===
# =================================================================
# These variables are REQUIRED for the application to run.
# When deployed to Cloud Run, these are automatically set when you
# connect your service to a Cloud SQL instance.
# For local testing, you must set these values manually.

DB_USER=your-db-username
DB_PASS=your-db-password
DB_NAME=your-database-name
INSTANCE_UNIX_SOCKET=/cloudsql/project-id:region:instance-id

# Note: The application will ONLY connect to PostgreSQL via Unix sockets.
# No fallback database connections are supported.

# =================================================================
# === GOOGLE / GEMINI CONFIGURATION ===
# =================================================================
GEMINI_API_KEY=your_gemini_api_key_here

# Google Cloud Storage bucket for transcripts
GCS_BUCKET_NAME=your-gcs-bucket-name

# =================================================================
# === EMAIL (2FA) CONFIGURATION ===
# =================================================================
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=ats7531331@gmail.com
MAIL_PASSWORD=zawxcvynwjuloatk
MAIL_DEFAULT_SENDER=ats7531331@gmail.com

# =================================================================
# === GOOGLE CLOUD STORAGE AUDIT LOGGING ===
# =================================================================
# Google Cloud Storage bucket for audit logs
AUDIT_LOG_BUCKET=your-audit-log-gcs-bucket

# Note: Google Cloud authentication is handled automatically in Cloud Run
# For local testing, you may need to set GOOGLE_APPLICATION_CREDENTIALS
# or use 'gcloud auth application-default login'

# =================================================================
# === SESSION / AUTH SETTINGS (OPTIONAL) ===
# =================================================================
# These have sensible defaults in code; set to override.
SESSION_TIMEOUT_MINUTES=1
MFA_CODE_EXPIRATION_MINUTES=10
